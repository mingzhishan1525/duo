{
  "title": "Website Contact Intelligence Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "Public company website URLs to inspect.",
      "editor": "requestListSources",
      "prefill": [
        {
          "url": "https://example.com"
        }
      ]
    },
    "maxPagesPerDomain": {
      "title": "Max pages per domain",
      "type": "integer",
      "description": "Maximum number of public pages to fetch per domain.",
      "minimum": 1,
      "maximum": 20,
      "default": 5
    },
    "maxConcurrency": {
      "title": "Max concurrency",
      "type": "integer",
      "description": "Maximum number of websites processed at the same time.",
      "minimum": 1,
      "maximum": 20,
      "default": 5
    },
    "respectRobotsTxt": {
      "title": "Respect robots.txt",
      "type": "boolean",
      "description": "When enabled, pages disallowed by robots.txt for this Actor user agent are skipped.",
      "default": true
    }
  },
  "required": [
    "startUrls"
  ]
}
